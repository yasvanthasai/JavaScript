oops
encap
anstraction
poly
inheritance

js is not oo language, but we can write oop code by using prototype object

oops module:
this keyword
constructor functionsclasses
classical interface
encapsulation
polyprototype object
prototypal inheritance
call apply bind


this keyword:
Node.js (Non-Strict Mode)

console.log(this) → {}
function → Global object
obj → fn → Object itself
obj → fn → fn → Global object

Node.js (Strict Mode)

console.log(this) → {}
function → undefined
obj → fn → Object itself
obj → fn → fn → undefined

Browser (Non-Strict Mode)

console.log(this) → window object
function → window object
obj → fn → Object itself
obj → fn → fn → window object

Browser (Strict Mode)

console.log(this) → window object
function → undefined
obj → fn → Object itself
obj → fn → fn → undefined

